# Copie para cloudflared/config.yml e preencha credentials-file apos o setup.
# Hostname fixo para dev local do HuginFlow (infra rn3.tec.br — rename DNS opcional).
# No .env.local use HUGINFLOW_WEBHOOK_URL_DEV apontando para este hostname.
#
# Setup: npm run tunnel:setup

tunnel: huginflow-dev-local
credentials-file: C:\Users\AlexandreNordin\.cloudflared\COLE-O-UUID-AQUI.json

ingress:
  - hostname: huginflow-local.rn3.tec.br
    service: http://localhost:3000
  - service: http_status:404
